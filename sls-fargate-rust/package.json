{
    "name": "sls-fargate-rust",
    "scripts": {
        "fmt": "cargo fmt",
        "lint": "cargo clippy",
        "compile": "cargo build",
        "build": "cargo build --release",
        "test": "cargo test",
        "package": "npx sls package",
        "deploy": "npx sls deploy",
        "invoke": "npx sls invoke -f func -d '{\"name\":\"Ben\"}'",
        "remove": "npx sls remove"
    },
    "devDependencies": {
        "serverless": "^3.34.0",
        "serverless-fargate": "^0.0.12"
    }
}
