{
    "name": "sls-lambda-docker-rust",
    "scripts": {
        "fmt": "cargo fmt",
        "lint": "cargo clippy",
        "compile": "cargo build",
        "build": "cargo build --release",
        "test": "cargo test",
        "invoke": "npx sls invoke -f func -d '{\"name\":\"Ben\"}'"
    },
    "devDependencies": {
        "serverless": "^3.34.0",
        "serverless-domain-manager": "^7.1.2"
    }
}
